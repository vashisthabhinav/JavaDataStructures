package trees;

public class Main {
    /*
    - Tree is a hierarchical data structure.
    - Tree has various nodes and nodes can have children and each node can have only one parent.
    - Every item in the tree is a node.
    - The node at the top of the tree is a root.
    - Every non-root node has one and only one parent.
    - A leaf node has no children(bottom-most node)
    - A singleton tree has only one node- the root

     */
}
